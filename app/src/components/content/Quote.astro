---
const { text, source } = Astro.props as {
  text: string;
  source: string;
};
---

<section class="quote-section">
  <blockquote class="exhibition-quote" aria-label="Zitat von Johann Peter Hebel">
    <p>{text}</p>
    <p>{source}</p>
  </blockquote>
</section>
